check host {{ wg_ipv4_subnet.split("/")[0] }} with address {{ wg_ipv4_subnet.split("/")[0] }}
  start program = "/usr/bin/wg-quick up wg0"
  stop program = "/usr/bin/wg-quick down wg0"
  if failed ping then restart